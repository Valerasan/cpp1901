#include <iostream>
#include <vector>

void printSize(const std::vector<auto> &myVec)
{
    std::cout<<"Vector length = "<<myVec.size()<<std::endl;
}

int main()
{
    
    
    
    
    std::vector<int> vec;
    vec.push_back(3);
    
    for (auto const 10 : vec)
        std::cout<< element<<' ';
        
        
    vec.push_back(5);
        
    
  
    

    return 0;
}